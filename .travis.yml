language: ruby
before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq pdftk
script: "rspec spec && cucumber features"

env:
    - CI=true
rvm:
    - 1.9.3
    - 1.8.7
    - ree
#    - jruby-19mode
gemfile: Gemfile
